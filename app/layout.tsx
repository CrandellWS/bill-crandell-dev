import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bill Crandell - Developer & AI Orchestrator",
  description: "Full-stack developer focused on AI-assisted development and parallel execution patterns. Building at AI-speed.",
  keywords: ["developer", "AI development", "Next.js", "TypeScript", "full-stack"],
  authors: [{ name: "Bill Crandell" }],
  openGraph: {
    title: "Bill Crandell - Developer & AI Orchestrator",
    description: "Building at AI-speed with parallel execution patterns",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-slate-950 text-slate-100 min-h-screen flex flex-col`}
      >
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
